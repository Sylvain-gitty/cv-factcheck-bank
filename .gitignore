# ---------------------------------------------------------------------------
# YOUR DATA NEVER GOES PUBLIC.
#
# This repo is the tooling. The fact bank it operates on contains your phone
# number, your email, your full employment history and your rendered CVs.
# Keep those local, or in a separate private repo.
#
# If you ever need to share a bank, share the .example.yaml files instead.
# ---------------------------------------------------------------------------
profile.yaml
entities.yaml
facts.yaml
vocab.yaml
confirm.txt
decisions.txt
NOTES.private.md

# Rendered output: CVs, traces, check reports
out/

# Your letters, the jobs you are applying to, and the dedup history
letters/
applications.csv
answers.yaml
jobs/*.yaml
!jobs/example-*.yaml
.state/

# LLM selector artefacts
llm_selection.json
prompt.txt

__pycache__/
*.pyc
.venv/
